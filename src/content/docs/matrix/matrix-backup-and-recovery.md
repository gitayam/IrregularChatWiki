---
title: "Matrix backup and recovery"
---

# Matrix backup and recovery

### Borg
NOTE: Borg needs to be installed on both systems

`ssh user@domain:port/./path/to/repo` will place this in the user’s dir

`ssh user@domain:port/path/to/repo` is the absolute path on the other system

### Database
